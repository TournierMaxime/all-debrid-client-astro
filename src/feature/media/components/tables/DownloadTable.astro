---
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"
import moment from "moment"
import Button from "@/components/shared/Button.astro"
import { Button as BtnShadcn } from "@/components/ui/button"
import type { Task } from "@/types/nas"
import { formatBytes } from "@/lib/utils"

const { task } = Astro.props
---

<Table className="bg-(--ads-bg-default) p-4 rounded-none">
  <TableHeader>
    <TableRow>
      <TableHead>ID</TableHead>
      <TableHead>Titre</TableHead>
      <TableHead>Status</TableHead>
      <TableHead>Taille</TableHead>
      <TableHead>Création</TableHead>
      <TableHead>Détails</TableHead>
      <TableHead>Supprimer</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    {
      task.data.tasks.map((t: Task) => (
        <TableRow>
          <TableCell>{t.id}</TableCell>
          <TableCell>{t.title}</TableCell>
          <TableCell>{t.status}</TableCell>
          <TableCell>{formatBytes(t.size)}</TableCell>
          <TableCell>
            {moment
              .unix(t.additional.detail.create_time)
              .format("DD/MM/YYYY à HH:mm")}
          </TableCell>
          <TableCell>
            <BtnShadcn
              size={"sm"}
              className="bg-(--ads-btn-default) rounded mr-4 cursor-pointer"
            >
              <a class="text-sm" href={`/download/${t.id}`}>
                Consulter
              </a>
            </BtnShadcn>
          </TableCell>
          <TableCell>
            <Button
              isDeleteBtn={true}
              onclick={`window.dispatchEvent(new CustomEvent("delete-task", { detail: { id: "${t.id}" } }))`}
              title="Supprimer"
            />
          </TableCell>
        </TableRow>
      ))
    }
  </TableBody>
</Table>
