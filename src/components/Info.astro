---
import Tag from "./Tag.astro"

interface Props {
  title?: string
  subtitle?: string
  data?: string | number
  dataMap?: { tag: string }[]
}

const { title, subtitle, data, dataMap } = Astro.props

const hasDataMap = Array.isArray(dataMap) && dataMap.length > 0
---

{title ? <h3 class="font-semibold text-sm">{title}</h3> : null}
{subtitle ? <h5 class="font-semibold text-sm">{subtitle}</h5> : null}

{
  hasDataMap && (
    <ul class="flex flex-row flex-wrap">
      {dataMap.map((d) => (
        <li>
          <Tag tag={d.tag} />
        </li>
      ))}
    </ul>
  )
}

{!hasDataMap && !data && <Tag tag="Non" />}

{data && <Tag tag={data} />}
